#include "TreeNode.h"
#include <iostream>
int main()
{
    BST tree;

    // tree.append(50LL);
    tree.append(30LL);
    tree.append(20LL);
    tree.append(25LL);
    tree.append(40LL);
    tree.append(10LL);
    // tree.deleteNode(tree.getRoot(), 40LL);
    // tree.BalanceTheTreeAfterDeletion(tree.getRoot());
    // tree.balanceTheFreakingTree(tree.getRoot(), tree.getNode(1));
    // std::cout << tree.getBalanceFactor(tree.getRoot());
    // tree.deleteNode(tree.getRoot(), 12);
    // std::cout << tree.Search(3);
    // std::cout << "PreOrder: ";
    // tree.preOrder(tree.getRoot());
    // tree.preOrder(tree.getRoot());
    // tree.preOrder(tree.getRoot());
    // std::cout << "\n";
    // std::cout << "PostOrder: ";
    // tree.postOrder(tree.getRoot());
    // std::cout << "\n";
    // std::cout << "inOrder: ";
    // tree.inOrder(tree.getRoot());
    // std::cout << tree.isEmpty() << std::endl;
    return 0;
}